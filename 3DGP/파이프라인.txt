디바이스/파이프라인 상태 변화 없이 뷰포트에 레스터라이저를 하는 것은 불가능
그리기 작업 3가지
1. 씬 전체 모든 오브젝트 그리기 -> 화면 전체
2. 디바이스/파이프라인 상태 변화
3. 다시 그리기

PSO(파이프라인 상태 객체)는 적어도 2개가 있어야 함

불투명한 것 그리기 -> 투명한 것 그리기 -> 블렌더 상태 설정 -> 최종 그리기

기하학적 데이터 = 모델

셋(set) - 드로우는 짝을 이루어야 함
셋을 하기 위해서는 쉐이더, 텍스쳐, 모델 등 필요 정보를 만들어야 함
모든 셋 함수는 커맨드 리스트에만 있음
